/*Секоj кандидат коj сака да се запише на факултет поднесува електронска приjава на системот за упис на Универзитетот. Потоа комисиjата за упис ги проверува
еден по еден кандидатите и нивните внесени податоци, а посебно го проверува
успехот на кандидатот од средно во апликациjата Е-Дневник (коjа содржи податоци за сите ученици во сите средни училишта во Македониjа). Ваша задача
е за даден кандидат да проверите дали е валиден внесениот просек од средно
училиште во апликациjата за уписи.
Влез: Во првата линиjа е даден броj 𝑁 на кандидати кои сакаат да се запишат
на факултет. Во наредните 𝑁 линии се дадени матичните броеви на кандидатите
и просек од средно образование коj го внеле во апликациjата за уписи. Потоа
е даден броj 𝑀 на податоци во Е-Дневник. Во наредните 𝑀 линии се дадени
матичните броеви на средношколците и нивниот вистински просек од средно
образование. Во последниот ред е даден матичниот броj на кандидатот чиj просек
треба да се провери.
Излез: Да се испечати дали кандидатот со дадениот матичен броj го внел
точниот просек од средно образование („OK”), дали просекот е погрешно внесен
(„Error”) или пак кандидатот воопшто го нема во Е-Дневник („Empty”).
Пример 1:
Влез:
2
0610992333666 5.0
0901993222233 4.78
231
Податочни структури
4
2205990121212 2.45
0901993222233 4.68
0610992333666 5.0
1511989984256 3.45
0901993222233
Излез:
Error
//или OK доколку е ОК
*/
//Секогаш кога имаме идентификатор кој се состои од цифри, но водечките нули се важни → чуваме го како String, а не int/long.

//int/long се користат само за бројки за аритметички операции, не за ID.

import java.util.Scanner;

public class FacultyEnrollment {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // 1. Вчитување број на кандидати
        int N = Integer.parseInt(sc.nextLine());//Првата линија од влезот ни кажува колку кандидати ќе внесеме.
       //sc.nextLine() вчитува ред како String, Integer.parseInt го претвора во број исто како int N=sc.nextInt(); sc.nextLine();

        // 2. Креирање hash табела за кандидатите
        OBHT<String, Double> candidates = new OBHT<>(2 * N);//Креираме OBHT со 2*N кофички → за да имаме доволно слободни места и помали шанси за collision.
       //Клучот е String (матичен број), вредноста е Double (просек).

        // 3. Вчитување кандидати и нивните внесени просеци
        for (int i = 0; i < N; i++) {
            String[] parts = sc.nextLine().split(" "); // делиме ред по празно место
            String pin = parts[0];                     // матичен број
            double avg = Double.parseDouble(parts[1]); // внесен просек
            candidates.insert(pin, avg);               // внесуваме во hash табела
        }

        // 4. Вчитување број на записи во Е-Дневник
        int M = Integer.parseInt(sc.nextLine());

        // 5. Креирање hash табела за Е-Дневник
        OBHT<String, Double> gradebook = new OBHT<>(2 * M);
      //Истото како кај кандидатите.Оваа табела ќе содржи вистински просек од Е-Дневник

        // 6. Вчитување матични броеви и вистински просеци од Е-Дневник
        for (int i = 0; i < M; i++) {
            String[] parts = sc.nextLine().split(" ");
            String pin = parts[0];
            double realAvg = Double.parseDouble(parts[1]);
            gradebook.insert(pin, realAvg);//insert ги хешира и додава во gradebook.Целта: да можеме да провериме дали внесениот просек е точен
        }

        // 7. Вчитување матичен број на кандидатот кој треба да се провери
        String PIN = sc.nextLine();//Овој ред е матичниот број на кандидатот кој треба да се провери

        // 8. Пребарување на кандидатот во hash табелата
        int candIndex = candidates.search(PIN);//пребарува во hash табелата candidates дали матичниот број PIN постои.
        if (candIndex != -1) { // ако постои кандидат,search проверува дали матичниот број постои во candidates,Враќа индекс на bucket ако постои, -1 ако не постои
        // Проверка дали кандидатот постои во листата на кандидати:Да → продолжуваме со проверка на просек,Не → System.out.println("Empty")
//✅ Значи, ако candIndex == -1 → кандидатот воопшто не се пријавил → Empty
          int gradeIndex = gradebook.search(PIN);
            if (gradeIndex != -1) { // ако постои и во Е-Дневник
                double candAvg = candidates.getBucket(candIndex).value;  // внесен просек,враќа MapEntry од hash табелата на индексот candIndex
                double realAvg = gradebook.getBucket(gradeIndex).value;  // вистински просек
                if (candAvg == realAvg) {
                    System.out.println("OK"); // точен внесен просек
                } else {
                    System.out.println("Error"); // погрешен внесен просек
                }
            } else {
                System.out.println("Empty"); // нема го нема во Е-Дневник
            }
        } else {
            System.out.println("Empty"); // кандидатот не постои во листата
        }

        sc.close(); // затвораме Scanner
    }
}
/*
<String, Double> → Ова се generic типови:

String → типот на key (клучот)

Double → типот на value (вредноста)

Во нашата задача:

key = матичен број на кандидатот (се чува како String, затоа што MAT број е 13 цифри, но не треба да се третира како број за аритметички операции)

value = просек од средно образование (Double, затоа што е десетична вредност како 4.78, 5.0 и сл.)

candidates = new OBHT<>(2 * N) → креираме hash табела со двојно повеќе кофички од бројот на кандидати, за да имаме доволно слободни позиции и помали шанси за collision.

parts[0] → делот од редот кој доаѓа пред првото празно место

Пример: редот "0610992333666 5.0" → split(" ") прави низа: ["0610992333666", "5.0"]

parts[0] = "0610992333666" → матичен број, се чува како String, затоа што:

Не ни треба да правиме аритметика со него

Долго е и има водечки нули, па ако го ставиме како int/long, нулите ќе се изгубат
double avg = Double.parseDouble(parts[1]);

parts[1] → делот од редот кој е просекот од средно

Double.parseDouble(parts[1]) → претвора String "5.0" во Double 5.0

Потребно е за да можеме да го споредиме или чува во hash табела како број

✅ Значи, на крај:

pin = "0610992333666" → key во hash табелата

avg = 5.0 → value во hash табелата

*/
